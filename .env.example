# Night Runner Configuration
# Copy this to .env and customize

# === Multiple Repositories (Recommended) ===
# Process multiple repos in one run
# Format: Each line is "repo_name|local_path"
REPO_LIST="
openonion/connectonion|$HOME/project/connectonion
openonion/night-runner|$HOME/project/night-runner
openonion/oo-api|$HOME/project/oo-api
"

# === Single Repository (Alternative) ===
# Comment out REPO_LIST above and use these for single repo
# REPO="openonion/connectonion"
# REPO_PATH="$HOME/project/connectonion"

# Max issues per run (0 = unlimited, processes all except those with "manual" label)
MAX_ISSUES=0

# Worktree base directory
WORKTREE_BASE="$HOME/worktrees"

# Claude CLI path (leave empty for default)
CLAUDE_PATH=""

# Timeout per issue (seconds)
TIMEOUT_SECONDS=1800

# Rate limit wait time (seconds)
RATE_LIMIT_WAIT=1800

# HTTP proxy (optional - only needed if Claude CLI requires proxy)
# http_proxy="http://localhost:8888"
# https_proxy="http://localhost:8888"
